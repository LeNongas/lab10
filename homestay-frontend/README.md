# Giao diện NEST Homestay

Giao diện React và Vite cho khách đặt phòng và quản trị viên. API chạy từ `../homestay-monolith` tại `http://localhost:8084`.

Chạy `npm install`, sau đó `npm run dev` và mở `http://localhost:5173`.
