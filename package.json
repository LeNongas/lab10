{
  "name": "homestay-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix homestay-frontend run dev",
    "build": "npm --prefix homestay-frontend run build",
    "lint": "npm --prefix homestay-frontend run lint"
  }
}
